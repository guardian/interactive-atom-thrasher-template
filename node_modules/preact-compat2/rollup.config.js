
import builder from "@daybrush/builder";
import preact from "rollup-plugin-preact";

const defaultConfig = {
    input: "src/index.ts",
    exports: "named",
    plugins: [
        preact({
            noPropTypes: true,
        }),
    ],
    external: {
        "preact": "preact",
        "preact-context": "preact-context",
    },
};
export default builder([
    {
        ...defaultConfig,
        output: "./dist/compat2.esm.js",
        format: "es",
    },
    {
        ...defaultConfig,
        output: "./dist/compat2.cjs.js",
        format: "cjs",
    },
]);
